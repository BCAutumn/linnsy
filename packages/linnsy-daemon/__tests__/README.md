# Tests

Package-level tests live here:

- `contract/` for public port contracts.
- `e2e/` for daemon flows.
- `harness/` for shared test helpers.
- `ui/`, `platform/`, and `chaos/` for later Sprint gates.
